# vis-autoclose

A plugin for [vis](https://git.sr.ht/~martanne/vis) which automatically closes opened brackets and also removes them when they are empty, when removing the opening bracket (using <Backspace>). Inspired by autoclose.vim.